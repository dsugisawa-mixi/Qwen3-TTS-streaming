services:
  # GPU を使うため、ホスト側に NVIDIA Container Toolkit が必要。
  # 未インストールだと `could not select device driver "nvidia" with capabilities: [[gpu]]` で起動失敗する。
  # セットアップ手順 (Ubuntu/Debian):
  #
  #   # 1. リポジトリ追加
  #   curl -fsSL https://nvidia.github.io/libnvidia-container/gpgkey | \
  #     sudo gpg --dearmor -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg
  #   curl -s -L https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list | \
  #     sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' | \
  #     sudo tee /etc/apt/sources.list.d/nvidia-container-toolkit.list
  #
  #   # 2. インストール
  #   sudo apt-get update
  #   sudo apt-get install -y nvidia-container-toolkit
  #
  #   # 3. Docker にランタイム登録 + 再起動
  #   sudo nvidia-ctk runtime configure --runtime=docker
  #   sudo systemctl restart docker
  qwen3-tts:
    image: 950363886025.dkr.ecr.ap-northeast-1.amazonaws.com/qwen3-tts-stream:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qwen3-tts
    restart: unless-stopped
    ports:
      - "8889:8889"
    volumes:
      - ./designed_references:/app/designed_references
      - ./pron_dict.json:/app/pron_dict.json
      - hf-cache:/root/.cache/huggingface
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_HOME=/root/.cache/huggingface
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    shm_size: "2gb"
    logging:
      driver: json-file
      options:
        max-size: "1m"
        max-file: "1"

  gpt-oss-llm:
    image: 950363886025.dkr.ecr.ap-northeast-1.amazonaws.com/gpt-oss-llm:latest
    container_name: gpt-oss-llm
    restart: unless-stopped
    ports:
      - "8081:8081"
    volumes:
      - ./professor_data:/app/professor_data
      - ./.apikeys.txt:/app/.apikeys.txt
      - ./.custom-auth.txt:/app/.custom-auth.txt
      - ../gpt-oss/.devices.txt:/app/.devices.txt
      - ../paper:/app/paper
      - hf-cache:/root/.cache/huggingface
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - HF_HOME=/root/.cache/huggingface
      - LAB_ID=lab-B
      - LAB_NAME=Arxiv(A)チーム
      - LAB_SUMMARY=Arxiv(cs.* and(WebRTC or QUIC or HTTP/3))
      # TTS は Compose 内部ネットワーク経由で qwen3-tts サービスへ。
      - TTS_BACKEND=http://qwen3-tts:8889
      # スライド生成パイプラインのスクリプト群。../paper を /app/paper にマウント済み。
      - SLIDE_GEN_SCRIPT=/app/paper/tools/note/generate_slides.py
      - SLIDE_HTML2PDF_SCRIPT=/app/paper/tools/note/html2pdf.mjs
      - SLIDE_SCRIPT_GEN_SCRIPT=/app/paper/tools/note/generate_scripts.py
      # スライド生成は gpt-oss-llm イメージの python3 を流用（openai 等の依存をイメージ側に含める前提）。
      - SLIDE_GEN_PYTHON=python3
      # ECS proxy → tunnel → llm の経路で配布される STS creds 注入用。
      # tunnel コンテナと同じ値を持つ必要がある。compose 内部ネットのみで使用。
      - INTERNAL_TOKEN=${INTERNAL_TOKEN:-poc-internal-token-change-me}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    shm_size: "2gb"
    logging:
      driver: json-file
      options:
        max-size: "1m"
        max-file: "1"

  tunnel:
    image: 950363886025.dkr.ecr.ap-northeast-1.amazonaws.com/proxy-tunnel:latest
    container_name: proxy-tunnel
    restart: unless-stopped
    # gpt-oss-llm へは Compose のデフォルトネットワーク経由でサービス名解決する。
    environment:
      - PROXY_WS_URL=ws://monst-proxy-1088691528.ap-northeast-1.elb.amazonaws.com/tunnel
      - LOCAL_URL=http://gpt-oss-llm:8081
      # gpt-oss-llm の /api/internal/aws-credentials を叩くための共有秘密。
      - INTERNAL_TOKEN=${INTERNAL_TOKEN:-poc-internal-token-change-me}
    logging:
      driver: json-file
      options:
        max-size: "1m"
        max-file: "1"

volumes:
  hf-cache:
